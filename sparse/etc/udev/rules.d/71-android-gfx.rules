# Android graphics memory allocator. gralloc takes every GPU buffer from here.
# phoc runs as a non-root user (droidian), so it has to be reachable by that
# user. On real Android ueventd makes this 0666; on Droidian udev has to.
KERNEL=="ion", MODE="0666"
