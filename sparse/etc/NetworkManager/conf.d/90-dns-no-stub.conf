# systemd-resolved is MASKED on this system (Droidian default), but
# NetworkManager still pointed resolv.conf at the resolved stub
# (127.0.0.53). With nothing listening there, DNS resolution failed
# completely. Write the real servers instead.
[main]
dns=default
rc-manager=unmanaged
