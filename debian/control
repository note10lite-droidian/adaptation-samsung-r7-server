Source: adaptation-samsung-r7
Maintainer: Yunus Tan <yunus.tan@outlook.com.tr>
Section: metapackages
Priority: optional
Build-Depends: debhelper (>= 10),
               debhelper-compat (= 13),
Standards-Version: 4.5.0.3

Package: adaptation-samsung-r7
Architecture: arm64
Depends: ${misc:Depends},
         adaptation-hybris-api33-phone,
         adaptation-samsung-r7-configs (= ${binary:Version}),
         openssh-server,
Description: Community adaptation for samsung-r7 - metapackage
 Pulls openssh-server so a first flash always has a working SSH server. The
 base image ships no SSH server at all, which left a fresh install with no
 remote access; SSH over Wi-Fi is the dependable fallback regardless of USB
 state.

Package: adaptation-samsung-r7-configs
Architecture: arm64
Depends: ${misc:Depends},
         python3-gi,
         python3-evdev,
         gir1.2-gtk-4.0,
         gir1.2-adw-1,
         gir1.2-gtk4layershell-1.0,
         libgtk4-layer-shell0,
         isc-dhcp-server,
         usbmuxd,
Description: Community adaptation for samsung-r7 - adaptation configurations
 Pulls isc-dhcp-server for its dhcpd binary, invoked directly by mtp-server's
 mtp-configfs script to serve an IP over the USB RNDIS gadget (10.15.19.0/24,
 see /etc/hybris-usb/dhcpd.conf); the package's own generic
 isc-dhcp-server.service is unused and disabled in postinst.
 .
 Pulls usbmuxd so USB-connected iOS devices (e.g. tethering to an iPhone's
 Personal Hotspot) get past the default PTP-only USB configuration: usbmuxd's
 own udev rule (39-usbmuxd.rules) activates it automatically on Apple vendor
 ID match, negotiates the device into its full configuration (PTP + Apple
 Mobile Device + Apple USB Ethernet), and ipheth (built into the kernel)
 binds the Ethernet interface from there - no adaptation-side wiring needed
 beyond having the package installed.

Package: adaptation-samsung-r7-desktop
Architecture: arm64
Depends: ${misc:Depends},
         adaptation-samsung-r7-configs (= ${binary:Version}),
         wayfire,
         waybar,
         fuzzel,
         foot,
         wlr-randr,
         iio-sensor-proxy,
Description: Community adaptation for samsung-r7 - desktop-shell (wayfire) mode
 Opt-in landscape desktop session for the r7 built on wayfire over the
 hwcomposer-patched wlroots. NOT enabled by default (phosh stays the shipped
 session); switch live with r7-switch-session. The config files (wayfire.ini,
 waybar, wayfire-test.service, r7-auto-rotate.service, r7-switch-session) ship
 inert in -configs; this metapackage only pulls the runtime stack
 (wayfire/waybar/fuzzel/foot/wlr-randr/iio-sensor-proxy).
 Notes: the 'decoration' plugin is deliberately omitted (it null-derefs the
 0.18-backport wlroots xdg lifecycle and crashes on toplevel map). Landscape
 rotation follows the accelerometer via r7-auto-rotate.service (net.hadess.
 SensorProxy -> wlr-randr --transform); no touch calibration matrix is used or
 needed, since wlroots already applies the output transform to absolute input
 devices internally (a matrix layered on top double-rotates touch).
